# Security policy

Report vulnerabilities privately to the repository owner rather than opening a public issue when the report could enable unauthorized GitHub actions, bypass an authority gate, expose credentials, or cross repository boundaries.

This project provides protocol and enforcement examples. It is not a sandbox, identity system, or replacement for host permissions. Test only on repositories and accounts you are authorized to use, with synthetic case state whenever possible.
